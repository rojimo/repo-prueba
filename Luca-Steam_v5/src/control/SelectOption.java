package control;

import utilities.Keyboard;

public class SelectOption {

	public static void selectOption() {
		/*do {
			try {
            	switch (Keyboard.saveInt()) {
                	case 1:
                    	//Alta de un juego
                    	services.registerGame();
                    	break;

                	case 2:
                    	//Obtener listado de los juegos existentes
                		services.listGames();
                		break;
                	case 3:
                    	//Obtener listado de los juegos filtrados por género
                    	services.obtainGenre();
                    	break;
            	}
            
        	} catch (Exception e) {
            	System.out.println("error: " + e.toString());
        	}
		} while((Keyboard.saveInt()<1) || (Keyboard.saveInt()>3));*/
    }
}
